{
  "name": "mcp-atlassian-e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prep": "playwright install --with-deps",
    "seed": "uv run python seed/seed.py",
    "test": "playwright test",
    "clean": "uv run python cleanup/cleanup.py"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0",
    "dotenv": "^16.4.5"
  }
}
