{
  "name": "mcp-atlassian-e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prep": "playwright install --with-deps && pip install -r requirements-test.txt",
    "start-server": "cd ../.. && uv run mcp-atlassian --transport streamable-http --port 9001",
    "seed": "./seed-with-server.sh",
    "seed:direct": "uv run python seed/seed.py",
    "test": "./run-tests-sequential.sh",
    "test:all": "./run-tests-sequential.sh",
    "test:mcp": "pytest -c pytest-mcp.ini -v",
    "test:visual": "pytest -c pytest-visual.ini -v",
    "test:api": "pytest -c pytest-mcp.ini -v -m api",
    "test:adf": "pytest -c pytest-mcp.ini -v -m adf",
    "test:jira": "pytest -c pytest-mcp.ini -v -m jira",
    "test:confluence": "pytest -c pytest-mcp.ini -v -m confluence",
    "test:smoke": "pytest -c pytest-mcp.ini -v -m smoke",
    "test:slow": "pytest -c pytest-mcp.ini -v -m slow",
    "test:parallel": "pytest -c pytest-mcp.ini -v -n auto",
    "test:legacy": "pytest -v",
    "clean": "uv run python cleanup/cleanup.py",
    "auth": "node manual-login.js",
    "auth-setup": "node manual-login.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.0",
    "dotenv": "^16.4.5"
  }
}
